@*@model IEnumerable<HopePipeline.Models.referralBrandi>
@{
    ViewData["Title"] = "viewBrandiR";
}

<h2>View For Referrals</h2>



<p>

</p>
<table class="table">
    <tr>
        <th>
            First Name
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.fName)
            </td>

            <td>





                @Html.ActionLink("Contact Info", "contactInfoBLD", "b", new { pK = item.pK }) |


                @Html.ActionLink("Details", "detailReferralBLD", "b", new { pK = item.pK })|

                @Html.ActionLink("Meeting Notes-Not Implemented Yet", "ContactInfo", "b", new { pK = item.pK })
            </td>
        </tr>
    }

</table>
<a asp-action="IndexA" asp-controller="b">Back to Homepage</a>*@
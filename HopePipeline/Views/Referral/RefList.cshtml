@model IEnumerable<HopePipeline.Models.Referral>

@{
    ViewData["Title"] = "Referral List";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";

}

<h2>Referrals</h2>

<table id="reflist" class="display">
    <thead>
        <tr>
            <th>
              Name
            </th>
            <th>
                Date of Birth
            </th>
            <th>
                Status
            </th>
            <th>
               More Options
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.lName) , @Html.DisplayFor(modelItem => item.fName)
            </td>
            <td>
               @Html.DisplayFor(modelItem => item.dOB)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.status)
            </td>
            <td>
                <a asp-controller="Tracking" asp-action="demoTracking">Start Tracking</a> |
                <a asp-controller="n" asp-action="detailReferralM">Details</a> |
                <a asp-controller="n" asp-action="contactInfoM">Contact</a> |
                <a asp-controller="Referral" asp-action="Delete" asp-route-pk="@item.pK">Delete</a>
            </td>
        </tr>; }
    </tbody>
</table>
